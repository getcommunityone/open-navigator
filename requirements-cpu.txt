# CPU-Only Requirements (No GPU/CUDA needed)
# Core Configuration
python-dotenv>=1.2.2
pydantic[email]>=2.5.0
pydantic-settings>=2.1.0

# Web Scraping & Data Collection
beautifulsoup4>=4.12.0
selenium>=4.16.0
playwright>=1.49.0
httpx>=0.25.0
crawl4ai>=0.4.0
litellm>=1.40.0
aiohttp>=3.14.0
youtube-transcript-api>=0.6.2

# AI & LLM (CPU versions)
openai>=1.6.0
anthropic>=0.8.0
langchain>=0.1.0
langchain-openai>=1.2.2
langchain-community>=0.0.10
langgraph>=0.0.20
langchain-core>=0.1.0
tiktoken>=0.5.0

# Databricks Agent Framework (Mosaic AI)
databricks-sdk>=0.18.0
mlflow>=2.10.0
databricks-agents>=0.1.0
databricks-vectorsearch>=0.22.0
# Spark + Delta Lake live in requirements-spark.txt (optional, requires Java).
# Install with: pip install -r requirements-spark.txt
pandas>=2.1.0
polars>=0.20.0
pyarrow>=14.0.0

# NLP - CPU-only versions (much lighter)
# Note: Omitting spacy, transformers, sentence-transformers, torch to save ~2GB
# You can add them back if needed: torch --index-url https://download.pytorch.org/whl/cpu
nltk>=3.9.4

# Database & Storage
sqlalchemy>=2.0.0
chromadb>=0.4.0
qdrant-client>=1.7.0

# API Framework
fastapi>=0.109.0
uvicorn[standard]>=0.25.0
# Removed fastapi-cache2 - not strictly required
python-multipart>=0.0.31

# Observability (OpenTelemetry tracing for FastAPI — see api/telemetry.py)
# Required at API startup; keep in sync with requirements.txt so CPU-only
# installs (install.sh / install.ps1 prefer this file) don't break on
# `import opentelemetry.instrumentation.fastapi`.
opentelemetry-api==1.42.0
opentelemetry-sdk==1.42.0
opentelemetry-instrumentation-fastapi==0.63b0
opentelemetry-exporter-otlp-proto-http==1.42.0

# Visualization
plotly>=5.18.0
folium>=0.15.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Utilities
python-dateutil>=2.8.0
pytz>=2023.3
tenacity>=8.2.0
loguru>=0.7.0
click>=8.1.0
tqdm>=4.66.3

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
pytest-httpx>=0.27.0

# Development
black>=23.0.0
ruff>=0.1.0
mypy>=1.8.0
pre-commit>=3.6.0

# Authentication & DB drivers (required at API startup — see api/auth.py, api/routes/*)
# Kept in sync with requirements.txt so CPU-only installs (install.sh prefers this
# file) don't break on `import jose` / `import asyncpg` at import time.
python-jose[cryptography]==3.4.0  # JWT tokens (3.4.0 fixes CVE-2024-33663 + CVE-2024-33664)
passlib[bcrypt]==1.7.4            # Password hashing
psycopg2-binary==2.9.9           # PostgreSQL driver
asyncpg==0.29.0                  # Async PostgreSQL driver (for Neon)
