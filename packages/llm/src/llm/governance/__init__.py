"""Governance meeting LLM pipeline (Gemma multimodal).

Ported out of ``scripts/colab/`` into the ``llm`` package. Runs the
governance-meeting analysis pipeline (gatekeeper triage → meeting grouping →
Gemma demos) in Google Colab, local Jupyter, or VS Code, with artifacts on
Google Drive. See ``README.md`` in this folder and the entry-point notebook
``02_run_meeting_llm.ipynb``.
"""
